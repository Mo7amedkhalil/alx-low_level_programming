#include "main.h"
#include <stdio.h>

/**
  * _isupper - checks chars if in upper case
  * @c: the input char
  *
  * Return: 1 if upper, 0 otherwise
  */

int _isupper(int c)
{
	return (isupper(c));
}
